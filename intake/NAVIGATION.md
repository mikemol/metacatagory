# Deprecated intake copy

Navigation is now generated. Please use:

* `docs/NAVIGATION.md` (produced by `make all` / `make docs-generate`) for the up-to-date map.
* Directory-level READMEs in `docs/` and `src/agda/**` for local context.

This intake file is retained only as a pointer; do not update it.***
