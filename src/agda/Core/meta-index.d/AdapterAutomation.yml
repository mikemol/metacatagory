title: 'Meta-Index: Core.AdapterAutomation'
sections:
- name: Scope
  bullets:
  - Categorical adapter patterns (standard/enhanced/legacy wrappers) and helpers for
    protocol migration.
- name: Key elements
  bullets:
  - '`HasCategorical` record; adapter variants `StandardAdapter`, `EnhancedAdapter`,
    `LegacyAdapterWrapper`.'
  - Constructors `mkStandardAdapter`, `mkEnhancedAdapter`, `upgradeLegacyAdapter`;
    check `isFilledStandard`.
- name: Dependencies
  bullets:
  - Infrastructure.Universe and Infrastructure.Coherence.Path2 (universe alias, equality/whiskering).
- name: Update triggers
  bullets:
  - Changes to adapter records or construction helpers.
  - Migration or categorical interface updates in Core.
  - Shifts in infrastructure equality/universe layers.
- name: Technical debt / status
  bullets:
  - "Universe polymorphism not yet generalized to `Set\u2113`; track in deferred-items.md\
    \ and ROADMAP.md."
